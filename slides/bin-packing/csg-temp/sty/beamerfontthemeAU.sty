\mode<presentation>

% use specific font
\RequirePackage{fontspec}
\setsansfont{Gilroy}[ 
Extension = .otf,
Path = csg-temp/fonts/,
UprightFont = *-Light,
BoldFont = *-Bold,
ItalicFont = *-LightItalic,
BoldItalicFont = *-BoldItalic
]
%\let\sfdefault\rmdefault

% bold monospace font
%\RequirePackage{bold-extra}                % does not seem to work (https://tex.stackexchange.com/questions/215482/how-do-i-get-texttt-with-bold-face-in-latex)
%\RequirePackage[scaled=0.85]{beramono}     % same here


\def\mathfamilydefault{\rmdefault}

\setbeamerfont{title}{size=\LARGE,series=\bfseries}
\setbeamerfont{frametitle}{size=\Large,series=\bfseries}
\setbeamerfont{framesubtitle}{size=\large}
\setbeamerfont{date}{size=\small}
\setbeamerfont{section title}{size=\LARGE}
\setbeamerfont{block title}{series=\bfseries}
\setbeamerfont{highlight title}{size=\Huge,series=\bfseries}
\setbeamerfont{section in toc}{series=\bfseries}

\setbeamerfont{footline}{size=\fontsize{5}{5}\selectfont}   % text in the footer

\setbeamerfont{bibliography item}{size=\footnotesize}
\setbeamerfont{bibliography entry author}{size=\footnotesize}
\setbeamerfont{bibliography entry title}{size=\footnotesize}
\setbeamerfont{bibliography entry location}{size=\footnotesize}
\setbeamerfont{bibliography entry note}{size=\footnotesize}
\renewcommand*{\bibfont}{\footnotesize}

% solid end of proof symbol
\setbeamertemplate{qed symbol}{$\blacksquare$}


% to simulate small caps with a custom font not supporting it
% retrieved from https://tex.stackexchange.com/a/225078/31360
\newcommand\fauxsc[1]{\fauxschelper#1 \relax\relax}
\def\fauxschelper#1 #2\relax{%
    \fauxschelphelp#1\relax\relax%
    \if\relax#2\relax\else\ \fauxschelper#2\relax\fi%
}
\def\Hscale{.83}\def\Vscale{.72}\def\Cscale{1.00}
\def\fauxschelphelp#1#2\relax{%
    \ifnum`#1>``\ifnum`#1<`\{\scalebox{\Hscale}[\Vscale]{\MakeUppercase{#1}}\else%
    \scalebox{\Cscale}[1]{#1}\fi\else\scalebox{\Cscale}[1]{#1}\fi%
    \ifx\relax#2\relax\else\fauxschelphelp#2\relax\fi}

\mode
<all>
